<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fitsane Registration</title>
    <link rel="stylesheet" href="/css/prac.css">
</head>
<body>
    <div class="logo">
       <h1><span id="f">FIT</span>SANE</h1>
    </div>
    <div class="register-container">
        <div class="box">
            
        <h2>Create Your Account</h2>
            <div class="form-group">
                <form action="/auth/register" method="post">
                <label for="name">Full Name</label>
            </div>
                <input type="text" id="name" name="name" placeholder="Enter your full name" value="<%= oldInput ? oldInput.name : '' %>" required>
            
            <div class="form-group">
                <label for="Age">Age</label>
            </div>
                <input type="text" id="Age" name="Age" placeholder="enter your Age" value="<%= oldInput ? oldInput.Age : '' %>"  required>
            <div class="form-group">
                Gender
            </div>
            <div class="form-group" id="wo">
                <select name="Gender" required>
                    <option value="">Select Gender</option>
                    <option value="Male" <%= oldInput && oldInput.Gender === 'Male' ? 'selected' : '' %>>Male</option>
                    <option value="Female" <%= oldInput && oldInput.Gender === 'Female' ? 'selected' : '' %>>Female</option>
                    <option value="Other" <%= oldInput && oldInput.Gender === 'Other' ? 'selected' : '' %>>Other</option>
                  </select>
            </div>
               
            
            <div class="form-group">
                <label for="password">Password</label>
            </div>
                <input type="password" id="password" name="password" placeholder="Enter your password" value="<%= oldInput ? oldInput.password : '' %>" required>
            
            <div class="form-group">
                <label for="Email">Email</label>
            </div>
                <input type="email" id="Email" name="Email" placeholder="Enter your email"  value="<%= oldInput ? oldInput.Email : '' %>" required>
            
            <button class="register-btn"><span id="regis">Register</span></button>
        </form>
        <p>Already have an account? <a href="fitsanelogin">Log in</a></p>
    </div>
</div>
<% if (error) { %>
    <script>alert("<%= error %>");</script>
  <% } %>
</body>
</html>
